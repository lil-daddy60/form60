
/* const randomNum = Math.floor(Math.random() * 50) + 1;
const userInput = document.querySelector(".userInput");
const btn = document.querySelector(".btn")
const toDisplay = document.querySelector(".toDisplay")

const userInteraction = () => {
    const userData = Number(userInput.value)
    if(userData === randomNum) {
        toDisplay.innerText = `Congrats! The number was ${randomNum}`
    }
    else if(userData > randomNum) {
       alert("Try lower")
    }
    else {
        alert("Try higher")
    }
}

btn.addEventListener("click", userInteraction) */